package com.teamresource.notification.domain.model;

public enum NotificationType {
    BOOKING_CONFIRMED,
    BOOKING_REJECTED,
    BOOKING_APPROVED,
    WAITLIST_PROMOTED,
    EVENT_REMINDER,
    BOOKING_CANCELLED,
    APPROVAL_RESULT,
    GENERIC
}
